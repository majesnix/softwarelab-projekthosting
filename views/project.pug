extends global/master
include includes/box

block content
    .col-md-12
        h1= activeProject.name
        .row
            .col-sm-6.col-lg-3
                +box("NodeJS", "test Test", "/node?id=" + activeProject.id)
            .col-sm-6.col-lg-3
                +box("Databases", "test Test", "/databases?id=" + activeProject.id)
            .col-sm-6.col-lg-3
                .box.box-default
                    .box-header.with-border
                        h3.box-title Add New
                    .box-body.text-center
                        // TODO Add Create
                        a(href="")
                            i.fa.fa-plus-circle.text-green
                            span Add New